/**
 * Domain model – entities and value objects.
 * <p>
 * REP: Reusable domain module; can be released with repository.
 * CCP: Model classes change together when domain rules or structure change.
 * CRP: Minimal dependencies; other layers depend on model, not the reverse.
 */
package model;
